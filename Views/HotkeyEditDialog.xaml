<Window x:Class="Sai2Capture.Views.HotkeyEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Sai2Capture.Views"
        xmlns:converters="clr-namespace:Sai2Capture.Converters"
        mc:Ignorable="d"
        Title="编辑热键"
        Height="260"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <!-- 转换器资源 -->
            <converters:BoolToEnabledBackgroundConverter x:Key="BoolToEnabledBackgroundConverter" />
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- 内容区 -->
        <Grid Grid.Row="0" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                         Text="快捷键："
                         VerticalAlignment="Center"
                         Margin="0,0,8,0"
                         FontSize="12"/>

                <Border Grid.Column="1"
                       Background="{Binding EditingHotkey.IsEnabled, Converter={StaticResource BoolToEnabledBackgroundConverter}}"
                       CornerRadius="4"
                       Padding="8,4">
                    <TextBlock Text="{Binding EditingHotkey.CurrentKey}"
                             VerticalAlignment="Center"
                             FontFamily="Consolas"
                             FontWeight="SemiBold"
                             FontSize="12"
                             HorizontalAlignment="Center"/>
                </Border>

                <ui:Button Grid.Column="2"
                         Content="捕捉"
                         Command="{Binding OpenHotkeyCaptureDialogCommand}"
                         Margin="8,0,0,0"
                         Style="{StaticResource RoundedUiButtonStyle}"/>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                         Text="启用："
                         VerticalAlignment="Center"
                         Margin="0,0,8,0"
                         FontSize="12"/>

                <ui:ToggleSwitch Grid.Column="1"
                             IsChecked="{Binding EditingHotkey.IsEnabled}"
                             VerticalAlignment="Center"/>
            </Grid>
        </Grid>

        <!-- 按钮区 -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="16,0">
                <ui:Button Content="确定"
                         Appearance="Primary"
                         Click="OKButton_Click"
                         Margin="0,0,8,0"
                         Style="{StaticResource RoundedUiButtonStyle}"/>

                <ui:Button Content="取消"
                         Appearance="Secondary"
                         Click="CancelButton_Click"
                         Style="{StaticResource RoundedUiButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>