<styles:CustomDialogWindow x:Class="Sai2Capture.Views.HotkeyErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:styles="clr-namespace:Sai2Capture.Styles"
        xmlns:local="clr-namespace:Sai2Capture.Views"
        mc:Ignorable="d"
        Title="错误"
        Height="180"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Topmost="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- 内容区 -->
        <Grid Grid.Row="0" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:SymbolIcon Grid.Row="0"
                          Symbol="Error24"
                          FontSize="24"
                          Foreground="#D13438"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,12"/>

            <TextBlock Grid.Row="1"
                     x:Name="MessageTextBlock"
                     Text="{Binding Message}"
                     TextWrapping="Wrap"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     FontSize="14"
                     Foreground="#323130"/>
        </Grid>

        <!-- 按钮区 -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="16,0">
                <ui:Button Content="确定"
                         Appearance="Primary"
                         Click="OKButton_Click"
                         Style="{StaticResource RoundedUiButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</styles:CustomDialogWindow>