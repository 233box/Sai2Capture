<styles:CustomMainWindow
    x:Class="Sai2Capture.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Sai2Capture"
    xmlns:styles="clr-namespace:Sai2Capture.Styles"
    xmlns:views="clr-namespace:Sai2Capture.Views"
    mc:Ignorable="d"
    Title="Sai2 捕获工具"
    Height="800"
    Width="600"
    Background="Transparent"
    WindowStyle="None"
    AllowsTransparency="True"
    ResizeMode="CanResize"
    Closing="Window_Closing">

    <!-- 主内容区域 -->
    <TabControl Margin="8,8,8,8" BorderThickness="0" Background="Transparent">
        <!-- 主页面标签页 -->
        <TabItem Header="主页面">
            <views:MainPage DataContext="{Binding}" Loaded="MainPage_Loaded"/>
        </TabItem>

        <!-- 设置标签页 -->
        <TabItem Header="设置">
            <views:SettingsPage DataContext="{Binding}"/>
        </TabItem>

        <!-- 系统日志标签页 -->
        <TabItem Header="系统日志">
            <views:LogPage x:Name="LogPageControl" DataContext="{Binding}"/>
        </TabItem>
    </TabControl>
</styles:CustomMainWindow>