<Window
    x:Class="Sai2Capture.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Sai2Capture"
    xmlns:views="clr-namespace:Sai2Capture.Views"
    mc:Ignorable="d"
    Title="Sai2 捕获工具"
    Height="450"
    Width="900"
    Background="Transparent"
    WindowStyle="None"
    AllowsTransparency="True"
    ResizeMode="CanResize"
    Closing="Window_Closing">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="28"
            CornerRadius="8"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </Window.Resources>

    <Border 
        Background="White" 
        CornerRadius="8"
        BorderBrush="#39C5BB"
        BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect 
                Color="Black" 
                Opacity="0.2" 
                BlurRadius="15" 
                ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border 
                Grid.Row="0" 
                Background="#39C5BB"
                CornerRadius="7,7,0,0"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 应用图标和标题 -->
                    <StackPanel 
                        Grid.Column="0" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center"
                        Margin="16,0,0,0">
                        <TextBlock 
                            Text="Sai2 捕获工具" 
                            FontSize="12" 
                            FontWeight="SemiBold"
                            Foreground="White"
                            VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- 窗口控制按钮 -->
                    <StackPanel 
                        Grid.Column="2" 
                        Orientation="Horizontal"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button 
                            x:Name="MinimizeButton"
                            Click="MinimizeButton_Click"
                            Style="{StaticResource WindowControlButtonStyle}"
                            Width="46" Height="28"
                            ToolTip="最小化">
                            <Path 
                                Data="M 0,0 H 10" 
                                Stroke="White" 
                                StrokeThickness="1"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                        </Button>
                        <Button 
                            x:Name="MaximizeButton"
                            Click="MaximizeButton_Click"
                            Style="{StaticResource WindowControlButtonStyle}"
                            Width="46" Height="28"
                            ToolTip="最大化">
                            <Path 
                                x:Name="MaximizeIcon"
                                Data="M 0,0 H 10 V 10 H 0 Z" 
                                Stroke="White" 
                                StrokeThickness="1"
                                Fill="Transparent"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                        </Button>
                        <Button 
                            x:Name="CloseButton"
                            Click="CloseButton_Click"
                            Style="{StaticResource CloseButtonStyle}"
                            Width="46" Height="28"
                            ToolTip="关闭">
                            <Path 
                                Data="M 0,0 L 10,10 M 10,0 L 0,10" 
                                Stroke="White" 
                                StrokeThickness="1"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主内容区域 -->
            <TabControl Grid.Row="1" Margin="8,8,8,8" BorderThickness="0" Background="Transparent">
                <!-- 主页面标签页 -->
                <TabItem Header="主页面">
                    <views:MainPage DataContext="{Binding}" Loaded="MainPage_Loaded"/>
                </TabItem>

                <!-- 设置标签页 -->
                <TabItem Header="设置">
                    <views:SettingsPage DataContext="{Binding}"/>
                </TabItem>

                <!-- 系统日志标签页 -->
                <TabItem Header="系统日志">
                    <views:LogPage x:Name="LogPageControl" DataContext="{Binding}"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</Window>